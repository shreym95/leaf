import { PlaceholderScreen } from "@/components/ui/PlaceholderScreen";

export default function LoginPage() {
  return (
    <PlaceholderScreen
      milestone="M1"
      title="Sign in"
      note="Google sign-in and your synced library arrive in M1."
    />
  );
}
